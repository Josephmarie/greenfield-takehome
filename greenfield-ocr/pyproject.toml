[project]
name = "greenfield-ocr"
version = "0.1.0"
description = "Fax intake OCR pipeline for Greenfield Cardiology"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.40.0",
    "pydantic>=2.0",
    "pdf2image>=1.17.0",
    "pillow>=10.0",
    "typer>=0.12.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
